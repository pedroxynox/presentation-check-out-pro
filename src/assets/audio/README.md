# Audio

Reserved space for audio cues and the soundtrack of the 120s experience.

- Register tracks through the `AudioManager` (see `src/components/core`).
- Prefer compressed formats (`.webm` / `.m4a` / `.mp3`) and provide multiple
  sources for browser compatibility.

> Placeholder file — replace with real audio assets as scenes are built.
