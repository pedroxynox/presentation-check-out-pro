# 3D Models

Reserved space for 3D assets (`.glb` / `.gltf`) used by the scenes.

Recommended workflow:

- Keep source `.glb`/`.gltf` here and load them with `useGLTF` from
  `@react-three/drei`.
- Draco/Meshopt-compress large models before committing.

> Placeholder file — replace with real model assets as scenes are built.
